{
  "homepage": "https://Inna-Mykytiuk.github.io/three-bees-animation",
  "scripts": {
    "build": "mkdir build && copy index.html build && copy style.css build && copy app.js build && xcopy img build\\img /E /I",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}