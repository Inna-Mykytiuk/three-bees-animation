{
  "homepage": "https://Inna-Mykytiuk.github.io/three-bees-animation",
  "scripts": {
    "build": "mkdir build && cp index.html build/ && cp style.css build/ && cp app.js build/ && cp -r img build/",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}