{
  "homepage": "https://Inna-Mykytiuk.github.io/three-bees-animation",
  "scripts": {
    "build": "cp index.html build/ && cp style.css build/ && cp app.js build/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}